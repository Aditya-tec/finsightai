GROQ_API_KEY=
GROQ_REPORT_MODEL=llama-3.3-70b-versatile
SUPABASE_URL=
SUPABASE_KEY=
COHERE_API_KEY=
API_KEY=
# Comma-separated browser origins allowed to call the API directly (local dev / Vercel).
# CORS_ORIGINS=https://finsightai-tau.vercel.app,http://localhost:3000
# Optional regex for Vercel preview deploys (default matches *.vercel.app).
# CORS_ORIGIN_REGEX=https://.*\.vercel\.app
HF_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
SUPABASE_FILINGS_BUCKET=annual-filings
# Leave unset in Docker/HF — defaults to backend/bm25_index.pkl and backend/graph.json
# BM25_INDEX_PATH=
# GRAPH_PATH=
